@using Microsoft.AspNetCore.Http
@inject ERPKardex.Services.IPermisoService _permisos

@{
    // Obtener controller y action para mantener tu lógica de "Active"
    string controllerName = ViewContext.RouteData.Values["Controller"]?.ToString();
    string actionName = ViewContext.RouteData.Values["Action"]?.ToString();
}
<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - SYSSAF</title>
    <script type="importmap"></script>
    <!-- AdminLTE -->
    <link rel="stylesheet" href="/css/adminlte.min.css">
    <link rel="stylesheet" href="/css/all.min.css">

    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css">

    <!-- SweetAlert2 -->
    <link rel="stylesheet" href="/plugins/sweetalert2/sweetalert2.css">
    <!-- Toastr -->
    <link rel="stylesheet" href="/plugins/toastr/toastr.min.css">
    <!-- Select2 -->
    <link rel="stylesheet" href="/plugins/select2/css/select2.min.css">
    <!-- Tempusdominus Bootstrap 4 -->
    <link rel="stylesheet" href="/plugins/tempusdominus-bootstrap-4/css/tempusdominus-bootstrap-4.min.css">
    <!-- Google Font: Source Sans Pro -->
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Source+Sans+Pro:300,400,400i,700">
    <!-- Datatables -->
    <link rel="stylesheet" href="/plugins/datatables-test/datatables.min.css">
    <!-- Select2 Bootstrap 4-->
    <link rel="stylesheet" href="/plugins/select2-bootstrap4-theme/select2-bootstrap4.css" />

    <!-- Scripts -->
    <!-- jQuery -->
    <script src="/js/jquery.min.js"></script>
    <!-- Bootstrap JS -->
    <script src="/js/bootstrap.bundle.min.js"></script>
    <!-- AdminLTE JS -->
    <script src="/js/adminlte.min.js"></script>

    <!-- Moment.js (para formateo de fechas) -->
    <script src="https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.29.4/moment.min.js"></script>

    <!-- SweetAlert2 -->
    <script src="/plugins/sweetalert2/sweetalert2.all.min.js"></script>
    <!-- Toastr -->
    <script src="/plugins/toastr/toastr.min.js"></script>
    <!-- Select2 -->
    <script src="/plugins/select2/js/select2.full.min.js"></script>
    <!-- InputMask -->
    <script src="/plugins/inputmask/jquery.inputmask.min.js"></script>
    <!-- Tempusdominus Bootstrap 4 -->
    <script src="/plugins/tempusdominus-bootstrap-4/js/tempusdominus-bootstrap-4.min.js"></script>
    <!-- Datatables -->
    <script src="/plugins/datatables-test/datatables.min.js"></script>
    <script>
        toastr.options = {
            "closeButton": true,
            "debug": false,
            "newestOnTop": true,
            "progressBar": true,
            "positionClass": "toast-top-right",
            "preventDuplicates": false,
            "onclick": null,
            "showDuration": "300",
            "hideDuration": "1000",
            "timeOut": "5000",
            "extendedTimeOut": "1000",
            "showEasing": "swing",
            "hideEasing": "linear",
            "showMethod": "fadeIn",
            "hideMethod": "fadeOut"
        };

        function loadBigModal(url, title) {
            $.ajax({
                url: url,
                type: 'GET',
                success: function (html) {
                    // Cambiar el contenido del modal
                    $('#dynamic-modal .modal-dialog').attr('class', 'modal-dialog modal-xl');
                    $('#modal-body').html(html);
                    $('#modal-title').text(title);
                    $('#dynamic-modal').modal('show');
                },
                error: function (xhr, status, error) {
                    console.error('Error al cargar el contenido del modal:', error);
                }
            });
        }

        function loadMediumModal(url, title) {
            $.ajax({
                url: url,
                type: 'GET',
                success: function (html) {
                    $('#dynamic-modal .modal-dialog').attr('class', 'modal-dialog modal-lg');
                    $('#modal-body').html(html);
                    $('#modal-title').text(title);
                    $('#dynamic-modal').modal('show');
                },
                error: function (xhr, status, error) {
                    console.error('Error al cargar el contenido del modal:', error);
                }
            });
        }

        function loadShortModal(url, title) {
            $.ajax({
                url: url,
                type: 'GET',
                success: function (html) {
                    $('#dynamic-modal .modal-dialog').attr('class', 'modal-dialog modal-sm');
                    $('#modal-body').html(html);
                    $('#modal-title').text(title);
                    $('#dynamic-modal').modal('show');
                },
                error: function (xhr, status, error) {
                    console.error('Error al cargar el contenido del modal:', error);
                }
            });
        }
    </script>
</head>
<body class="hold-transition sidebar-mini layout-footer-fixed">
    <div class="wrapper">
        <!-- Preloader -->
        <!-- <div class="preloader flex-column justify-content-center align-items-center">
            <img class="animation__shake" src="/Static/img/yatrax-preloader.png" alt="YatraxLogo" height="120" width="200">
        </div> -->
        <!-- Navbar -->
         <nav class="main-header navbar navbar-expand navbar-white navbar-light" id="maestra_header">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" data-widget="pushmenu" href="#" role="button" id="btn_sidebar">
                        <i class="fas fa-bars"></i>
                    </a>
                </li>
                <li class="nav-item d-none d-sm-inline-block">
                    <a href="#" class="nav-link">Inicio</a>
                </li>
            </ul>

            <!-- Botón de Logout -->
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <form action="/Home/Logout" method="post">
                        <button type="submit" class="btn btn-danger">
                            <i class="fas fa-sign-out-alt"></i> Cerrar sesión
                        </button>
                    </form>
                </li>
            </ul>
        </nav> 

        <!-- Sidebar -->
         <aside class="main-sidebar sidebar-dark-primary elevation-4">
            <!-- Brand Logo -->
            <a href="" class="brand-link">
                <img src="/uploads/logo/Ynnovatech.jpg" alt="SYSSAF" class="brand-image img-circle elevation-3" style="opacity: .8">
                <span class="brand-text font-weight-light">SYSSAF</span>
            </a>

            <!-- Sidebar -->
            <div class="sidebar">
                <!-- Sidebar user panel (optional) -->
                <div class="user-panel mt-3 pb-3 mb-3 d-flex">
                    <div class="image">
                        <img src="/uploads/fotos/default-user.png" class="img-circle elevation-2" alt="User Image">
                    </div>
                    <div class="info">
                        <a href="#" class="d-block font-weight-bold">@User.Identity.Name</a>

                        <span class="text-muted d-block" style="font-size: 0.85rem;">
                            <i class="fas fa-building mr-1"></i>
                            @(User.FindFirst("RazonSocial")?.Value ?? "Sin Empresa")
                        </span>
                    </div>
                </div>

                <nav class="mt-2">
                    <ul class="nav nav-pills nav-sidebar flex-column" data-widget="treeview" role="menu" data-accordion="false">

                        @if (await _permisos.TienePermiso("MOD_MAESTROS"))
                        {
                            <li class="nav-header font-weight-bold">MÓDULO MAESTROS</li>

                            bool isMaestrosOpen = (controllerName == "Proveedor") ||
                            (controllerName == "Cliente") ||
                            (controllerName == "TipoCambio") ||
                            (controllerName == "CentroCosto") ||
                            (controllerName == "Producto");

                            <li class="nav-item @(isMaestrosOpen ? "menu-open" : "")">
                                <a href="#" class="nav-link @(isMaestrosOpen ? "active" : "")">
                                    <i class="nav-icon fas fa-database"></i>
                                    <p>Registros Maestros <i class="right fas fa-angle-left"></i></p>
                                </a>
                                <ul class="nav nav-treeview">
                                    @if (await _permisos.TienePermiso("MAE_PROD_REG"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Producto/Registrar" class="nav-link @((controllerName == "Producto" && actionName == "Registrar") ? "active" : "")">
                                                <i class="fas fa-box nav-icon text-info"></i><p>Nuevo Producto</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("MAE_SERV_REG"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Producto/RegistroServicio" class="nav-link @((controllerName == "Producto" && actionName == "RegistroServicio") ? "active" : "")">
                                                <i class="fas fa-tools nav-icon text-info"></i><p>Nuevo Servicio</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("MAE_PROV_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Proveedor/Index" class="nav-link @((controllerName == "Proveedor") ? "active" : "")">
                                                <i class="fas fa-truck nav-icon"></i><p>Proveedores</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("MAE_CLIE_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Cliente/Index" class="nav-link @((controllerName == "Cliente") ? "active" : "")">
                                                <i class="fas fa-users nav-icon"></i><p>Clientes</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("MAE_TC_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/TipoCambio/Index" class="nav-link @((controllerName == "TipoCambio") ? "active" : "")">
                                                <i class="fas fa-money-bill-wave nav-icon text-success"></i><p>Tipo de Cambio</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("MAE_CC_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/CentroCosto/Index" class="nav-link @((controllerName == "CentroCosto") ? "active" : "")">
                                                <i class="fas fa-sitemap nav-icon text-warning"></i><p>Centros de Costo</p>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </li>
                        }

                        @if (await _permisos.TienePermiso("MOD_SUPPLY"))
                        {
                            <li class="nav-header font-weight-bold">MÓDULO SUPPLY</li>

                            bool isInvOpen = (controllerName == "IngresoSalidaAlm") || (controllerName == "Producto" && actionName == "Index");

                            <li class="nav-item @(isInvOpen ? "menu-open" : "")">
                                <a href="#" class="nav-link @(isInvOpen ? "active" : "")">
                                    <i class="nav-icon fas fa-warehouse"></i>
                                    <p>Control de Inventario <i class="right fas fa-angle-left"></i></p>
                                </a>
                                <ul class="nav nav-treeview">
                                    @if (await _permisos.TienePermiso("SUP_INV_MOV_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/IngresoSalidaAlm/Index" class="nav-link @((controllerName == "IngresoSalidaAlm" && new[] { "Index", "Registrar" }.Contains(actionName)) ? "active" : "")">
                                                <i class="fas fa-exchange-alt nav-icon"></i><p>Ingresos y Salidas</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_INV_MOV_VALORIZAR"))
                                    {
                                        <li class="nav-item">
                                            <a href="/IngresoSalidaAlm/Valorizacion" class="nav-link @(controllerName == "IngresoSalidaAlm" && actionName == "Valorizacion" ? "active" : "")">
                                                <i class="fas fa-file-invoice-dollar nav-icon"></i><p>Valorizar movimiento</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_INV_STOCK_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/IngresoSalidaAlm/ReporteStock" class="nav-link @((controllerName == "IngresoSalidaAlm" && actionName == "ReporteStock") ? "active" : "")">
                                                <i class="fas fa-boxes nav-icon"></i><p>Reporte de Stock</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_INV_KARDEX_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/IngresoSalidaAlm/ReporteKardex" class="nav-link @((controllerName == "IngresoSalidaAlm" && actionName == "ReporteKardex") ? "active" : "")">
                                                <i class="fas fa-chart-line nav-icon"></i><p>Reporte Kardex</p>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </li>

                            bool isLogOpen = new[] { "Logistica", "ReqCompra", "ReqServicio", "PedCompra", "PedServicio", "OrdenCompra", "OrdenServicio" }.Contains(controllerName);

                            <li class="nav-item @(isLogOpen ? "menu-open" : "")">
                                <a href="#" class="nav-link @(isLogOpen ? "active" : "")">
                                    <i class="nav-icon fas fa-truck-loading"></i>
                                    <p>Logística <i class="right fas fa-angle-left"></i></p>
                                </a>
                                <ul class="nav nav-treeview">

                                    @if (await _permisos.TienePermiso("SUP_LOG_DASH_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Logistica/Dashboard" class="nav-link @(controllerName == "Logistica" ? "active" : "")">
                                                <i class="fas fa-chart-line nav-icon"></i><p>Dashboard Logístico</p>
                                            </a>
                                        </li>
                                    }

                                    @if (await _permisos.TienePermiso("SUP_LOG_REQ_COMPRA_VER") || await _permisos.TienePermiso("SUP_LOG_ORD_COMPRA_VER"))
                                    {
                                        <li class="nav-header pl-3 text-uppercase text-secondary small font-weight-bold mt-2" style="font-size: 0.7rem;">Gestión de Compras</li>
                                    }

                                    @if (await _permisos.TienePermiso("SUP_LOG_REQ_COMPRA_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/ReqCompra/Index" class="nav-link @(controllerName == "ReqCompra" ? "active" : "")">
                                                <i class="fas fa-cart-plus nav-icon"></i><p>Req. de Compra</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_LOG_PED_COMPRA_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/PedCompra/Index" class="nav-link @(controllerName == "PedCompra" ? "active" : "")">
                                                <i class="fas fa-shopping-cart nav-icon"></i><p>Pedido de Compra</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_LOG_ORD_COMPRA_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/OrdenCompra/Index" class="nav-link @(controllerName == "OrdenCompra" ? "active" : "")">
                                                <i class="fas fa-file-invoice-dollar nav-icon"></i><p>Orden de Compra</p>
                                            </a>
                                        </li>
                                    }

                                    @if (await _permisos.TienePermiso("SUP_LOG_REQ_SERV_VER") || await _permisos.TienePermiso("SUP_LOG_ORD_SERV_VER"))
                                    {
                                        <li class="nav-header pl-3 text-uppercase text-secondary small font-weight-bold mt-2" style="font-size: 0.7rem;">Gestión de Servicios</li>
                                    }

                                    @if (await _permisos.TienePermiso("SUP_LOG_REQ_SERV_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/ReqServicio/Index" class="nav-link @(controllerName == "ReqServicio" ? "active" : "")">
                                                <i class="fas fa-tools nav-icon"></i><p>Req. de Servicio</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_LOG_PED_SERV_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/PedServicio/Index" class="nav-link @(controllerName == "PedServicio" ? "active" : "")">
                                                <i class="fas fa-concierge-bell nav-icon"></i><p>Pedido de Servicio</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("SUP_LOG_ORD_SERV_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/OrdenServicio/Index" class="nav-link @(controllerName == "OrdenServicio" ? "active" : "")">
                                                <i class="fas fa-file-signature nav-icon"></i><p>Orden de Servicio</p>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </li>
                        }

                        @if (await _permisos.TienePermiso("MOD_PROVISIONES"))
                        {
                            <li class="nav-header font-weight-bold">MÓDULO PROVISIONES</li>

                            bool isProvOpen = (controllerName == "CuentasPorPagar");
                            <li class="nav-item @(isProvOpen ? "menu-open" : "")">
                                <a href="#" class="nav-link @(isProvOpen ? "active" : "")">
                                    <i class="nav-icon fas fa-file-invoice-dollar"></i>
                                    <p>Cuentas por Pagar <i class="right fas fa-angle-left"></i></p>
                                </a>
                                <ul class="nav nav-treeview">
                                    @if (await _permisos.TienePermiso("PROV_ANT_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/CuentasPorPagar/IndexAnticipos" class="nav-link @(actionName.Contains("Anticipo") ? "active" : "")">
                                                <i class="fas fa-hand-holding-usd nav-icon"></i><p>Anticipos</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("PROV_REG_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/CuentasPorPagar/IndexProvisiones" class="nav-link @(actionName.Contains("Provision") ? "active" : "")">
                                                <i class="fas fa-file-contract nav-icon"></i><p>Provisiones</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("PROV_NOTA_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/CuentasPorPagar/IndexNotas" class="nav-link @(actionName.Contains("Nota") ? "active" : "")">
                                                <i class="fas fa-sticky-note nav-icon"></i><p>Notas Crédito/Débito</p>
                                            </a>
                                        </li>
                                    }
                                    @if (await _permisos.TienePermiso("PROV_APP_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/CuentasPorPagar/AplicacionDocumentos" class="nav-link @(actionName == "AplicacionDocumentos" ? "active" : "")">
                                                <i class="fas fa-exchange-alt nav-icon"></i><p>Aplicación Anticipos</p>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </li>
                        }

                        @if (await _permisos.TienePermiso("MOD_TESORERIA"))
                        {
                            <li class="nav-header font-weight-bold">MÓDULO TESORERÍA</li>

                            @if (await _permisos.TienePermiso("TES_PAGO_VER"))
                            {
                                <li class="nav-item">
                                    <a href="/OrdenPago/Index" class="nav-link @((controllerName == "OrdenPago") ? "active" : "")">
                                        <i class="fas fa-money-check-alt nav-icon"></i>
                                        <p>Gestión de Pagos</p>
                                    </a>
                                </li>
                            }
                        }

                        @if (await _permisos.TienePermiso("MOD_ACTIVOS"))
                        {
                            <li class="nav-header font-weight-bold">MÓDULO ACTIVO FIJO</li>

                            // Capturamos el parámetro 'tipo' de la URL para saber qué sub-menú activar
                            string currentTipo = Context.Request.Query["tipo"].ToString().ToUpper();
                            // Variables auxiliares para limpieza
                            bool isActivoController = controllerName == "Activo";

                            <li class="nav-item @(isActivoController ? "menu-open" : "")">
                                <a href="#" class="nav-link @(isActivoController ? "active" : "")">
                                    <i class="nav-icon fas fa-cubes"></i>
                                    <p>
                                        Activos Fijos
                                        <i class="right fas fa-angle-left"></i>
                                    </p>
                                </a>
                                <ul class="nav nav-treeview">
            
                                    @* 1. DASHBOARD (Si aplica) 
                                    @if (await _permisos.TienePermiso("ACT_DASH_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Activo/Dashboard" class="nav-link @(isActivoController && actionName == "Dashboard" ? "active" : "")">
                                                <i class="nav-icon fas fa-tachometer-alt text-danger"></i>
                                                <p>Dashboard</p>
                                            </a>
                                        </li>
                                    }*@

                                    @* 2. INVENTARIO CÓMPUTO *@
                                    @if (await _permisos.TienePermiso("ACT_COMPUTO_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Activo/Index" class="nav-link @(isActivoController && actionName == "Index" ? "active" : "")">
                                                <i class="fas fa-laptop nav-icon text-primary"></i>
                                                <p>Inventario Cómputo</p>
                                            </a>
                                        </li>
                                    }

                                    @* 4. INVENTARIO FLOTA *@
                                    @if (await _permisos.TienePermiso("ACT_FLOTA_VER"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Activo/Vehiculos" class="nav-link @(isActivoController && actionName == "Vehiculos" ? "active" : "")">
                                                <i class="fas fa-truck nav-icon text-warning"></i>
                                                <p>Flota Vehicular</p>
                                            </a>
                                        </li>
                                    }

                                    @* 5. MOVIMIENTOS FLOTA (Usa acción Movimientos con param) *@
                                    @if (await _permisos.TienePermiso("ACT_MOV"))
                                    {
                                        <li class="nav-item">
                                            <a href="/Activo/Movimientos" class="nav-link @(isActivoController && actionName == "Movimientos" ? "active" : "")">
                                                <i class="fas fa-exchange-alt nav-icon text-warning"></i>
                                                <p>Movimientos</p>
                                            </a>
                                        </li>
                                    }
                                </ul>
                            </li>
                        }

                    </ul>
                </nav>
                <!-- /.sidebar-menu -->
            </div>
            <!-- /.sidebar -->
        </aside> 

        <!-- Contenido principal -->
        <div class="content-wrapper">
            @RenderBody()
        </div>
    </div>

    <div id="dynamic-modal" class="modal fade" style="display: none;">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <h4 id="modal-title" class="modal-title">Default Modal</h4>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                        <span>×</span>
                    </button>
                </div>
                <div id="modal-body" class="modal-body">
                    <p>One fine body…</p>
                </div>
            </div>
            <!-- /.modal-content -->
        </div>
        <!-- /.modal-dialog -->
    </div>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
